## Node+Ts
### 搭建环境
* `@types/node`和`@types/express`都是为了让TS知道有这些API，主要是代替识别文件。  
* 配置文件命令，在tsc的同时运行server.js文件
* 配置命令`tsc&&node build/server.js`,`nodemon -e ts -w ./ -x npm run build`
### 搭建Restful API
